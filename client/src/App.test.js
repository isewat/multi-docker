import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

it('renders without crashing', () => {
  // Don't test this because of dependencies to Redis & Postgres
  // TODO: set up faker module to mock JSON data to render
});
